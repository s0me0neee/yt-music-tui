[package]
name = "yt-music-tui"
version = "0.1.0"
edition = "2024"
#WARN:remove secret and clientid when puslished, and publish in a new repo to pervent history leak

[dependencies]
ratatui = "0.30.0"
anyhow = "1"
serde_json = "1"
ytmusicapi = { path = "../ytmusicapi" }
log = "0.4"
simplelog = "0.12"
