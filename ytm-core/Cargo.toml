[package]
name = "ytm-core"
version = "0.1.0"
edition = "2024"

[dependencies]
lrclib = { path = "../lrclib" }
ytmusicapi = "0.4.2"
libmpv2 = "6.0.0"
# Lyric translation. Wraps Google's public translate_a/single endpoint; brings
# no HTTP stack of its own — reqwest 0.12.4 resolves to the 0.12 already in the
# graph, so `cargo tree -i aws-lc-sys` still reports *not found*. It does ask
# for tokio's "full" feature set, which unifies across the workspace and costs
# build time but nothing at runtime. See `translate.rs` for what it gets wrong
# and how that is handled.
rust-translate = "0.1.3"
# "sync" is not needed: concurrency in `translate.rs` is bounded by spawning in
# rounds rather than with a semaphore.
tokio = { version = "1", features = ["rt", "time"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# config.toml parsing. Read-only, so no "display" feature.
toml = { version = "0.8.23", default-features = false, features = ["parse"] }
# Format-preserving edits: the app records the browser that worked back into
# config.toml, which is hand-edited and full of comments worth keeping.
toml_edit = "0.22.27"
thiserror = "1"
log = "0.4"
rand = "0.8"
dirs = "5"
inquire = "0.9.4"
hotpath = "0.19.0"

[dev-dependencies]
# `macros` for #[tokio::test] in the live lyrics tests; `test-util` for the
# paused clock that keeps the retry-backoff tests instant.
tokio = { version = "1", features = [
    "rt",
    "rt-multi-thread",
    "macros",
    "time",
    "test-util",
] }

[build-dependencies]
pkg-config = "0.3.33"

# Profiling — zero overhead unless one of these is enabled.
# Forwarded through by the `tui` binary's identically-named features.
[features]
hotpath = ["hotpath/hotpath"]
hotpath-alloc = ["hotpath", "hotpath/hotpath-alloc"]
hotpath-cpu = ["hotpath", "hotpath/hotpath-cpu"]
